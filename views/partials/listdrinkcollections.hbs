<section class="ui left aligned middle aligned segment">

  {{> dashboardHeader }}

  {{#unless search}}
    {{#unless drinks}}
    <h3 class="ui message">
      Start adding your drink collections!
    </h3>
    {{/unless}}
  {{/unless}}

  <div class="ui centered cards">
    
    {{#each drinks}}
      {{> drinkCollectionCard}}
    {{/each}}
    
  </div>

</section>
